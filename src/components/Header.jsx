import React from 'react';
import MenuAppBar from './MenuAppBar';

const Header = () => {
  return (
    <header>
      <nav>
        <MenuAppBar />
      </nav>
    </header>
  );
};

export default Header;
