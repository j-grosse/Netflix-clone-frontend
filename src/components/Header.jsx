import React from 'react';
import SearchAppBar from './SearchAppBar';

const Header = () => {
  return (
    <header>
      <nav>
        <SearchAppBar />
      </nav>
    </header>
  );
};

export default Header;
